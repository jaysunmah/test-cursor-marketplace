# Release Ops Control Center

A Cursor plugin fixture demonstrating a plugin that uses both `hooks` and `mcpServers` optional manifest fields.

## Included Components

- A manifest that explicitly declares both optional fields
- A release-oriented MCP server config stub
- Hook definitions for session start and session end
- A small skill, command, and agent to make the fixture realistic
