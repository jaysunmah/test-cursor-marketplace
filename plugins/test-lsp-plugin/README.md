# Test LSP Plugin

Test plugin demonstrating an LSP-oriented fixture for TypeScript and JavaScript workflows.

## Notes

This plugin is kept as a lightweight Cursor marketplace fixture. The Cursor plugin docs focus on `.cursor-plugin/plugin.json` manifests and plugin-local assets, so this example keeps the metadata minimal while preserving the LSP-oriented example content.
